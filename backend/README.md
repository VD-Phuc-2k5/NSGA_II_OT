# Backend - Doctor Scheduling API

## Run

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

The backend reuses the improved NSGA-II implementation from the root package `nsga2_improved`.
